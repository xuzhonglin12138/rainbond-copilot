# rainbond-static-package

Minimal static package for Rainbond package-build verification.

## Build

```bash
npm run build
```

The build writes `dist/index.html`, which matches the default static-package
settings Rainbond detected during the upload flow.
